# AI-Mem Backend Environment Configuration

# Server Settings
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Security
SECRET_KEY=your-secret-key-change-in-production-minimum-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database Configuration
# PostgreSQL (recommended for production/Docker)
DATABASE_URL=postgresql+asyncpg://aimem_user:aimem_password@localhost:5432/aimem

# SQLite fallback for local development
# DATABASE_URL=sqlite+aiosqlite:///./aimem.db

# Development PostgreSQL (docker-compose.dev.yml)
# DATABASE_URL=postgresql+asyncpg://dev_user:dev_password@localhost:5433/aimem_dev

# Redis Configuration
REDIS_URL=redis://localhost:6379

# Development Redis (docker-compose.dev.yml)
# REDIS_URL=redis://localhost:6380

# Search Configuration
SEARCH_MODEL_NAME=all-MiniLM-L6-v2

# File Upload
MAX_FILE_SIZE=10485760  # 10MB in bytes

# Logging
LOG_LEVEL=INFO

# CORS Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001